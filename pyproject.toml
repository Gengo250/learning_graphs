[project]
name = "grafos-network"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gengo250", email = "miguelgengo28@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "networkx>=3.6.1",
]

[project.scripts]
grafos-network = "grafos_network:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
